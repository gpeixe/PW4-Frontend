import React, { Component } from 'react';
import { withRouter } from 'react-router';

class SummonerPage extends Component {
  render(){
      return <div>Summoner's Page</div>
  }
}

export default withRouter(SummonerPage);